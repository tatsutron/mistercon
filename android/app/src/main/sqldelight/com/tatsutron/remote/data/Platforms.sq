CREATE TABLE Platforms (
    name TEXT NOT NULL PRIMARY KEY
);

save:
REPLACE INTO Platforms(name)
VALUES (:name);

selectAll:
SELECT * FROM Platforms;

selectByName:
SELECT * FROM Platforms WHERE name = ?;

clear:
DELETE FROM Platforms;
