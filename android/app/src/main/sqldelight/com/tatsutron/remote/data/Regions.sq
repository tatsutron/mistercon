CREATE TABLE IF NOT EXISTS Regions (
    regionID INTEGER PRIMARY KEY AUTOINCREMENT,
    regionName TEXT
);

selectBySha1:
SELECT *
FROM Regions
WHERE regionID = (
    SELECT regionID
    FROM Roms
    WHERE romHashSHA1 = ?
)
LIMIT 1;
