CREATE TABLE Config (
    host TEXT NOT NULL,
    id INTEGER PRIMARY KEY CHECK (id = 0)
);

select:
SELECT * FROM Config WHERE id='0';

save:
REPLACE INTO Config(
    host,
    id
)
VALUES (
    :host,
    :id
);
