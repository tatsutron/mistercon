CREATE TABLE Consoles ( -- TODO Rename to `Platforms`
    gamesPath TEXT NOT NULL,
    name TEXT PRIMARY KEY
);

save:
REPLACE INTO Consoles(gamesPath, name)
VALUES (:gamesPath, :name);

selectByName:
SELECT * FROM Consoles WHERE name = ?;
