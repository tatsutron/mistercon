CREATE TABLE Config (
    host TEXT NOT NULL,
    id INTEGER PRIMARY KEY CHECK (id = 0),
    scriptsPath TEXT NOT NULL
);

select:
SELECT * FROM Config WHERE id='0';

save:
REPLACE INTO Config(host, id, scriptsPath)
VALUES (:host, :id, :scriptsPath);
